name: Docs — GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'docs/**'
      - 'mkdocs.yml'
      - 'requirements.txt'
      - 'structurizr/**'
      - '.github/workflows/docs.yml'
  workflow_run:
    workflows: [CI]
    branches: [main]
    types: [completed]
  workflow_dispatch:

permissions:
  actions: read
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    name: Build documentação
    runs-on: ubuntu-latest
    # Quando disparado por workflow_run, só continua se o CI passou
    if: >-
      github.event_name != 'workflow_run' ||
      github.event.workflow_run.conclusion == 'success'

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip

      - name: Instalar dependências
        run: pip install -r requirements.txt

      - name: Configurar GitHub Pages
        uses: actions/configure-pages@v5

      - name: Build MkDocs
        run: mkdocs build --verbose

      # ── Structurizr → HTML estático ─────────────────────────────────────────
      # structurizr/cli está descontinuado; site-generatr (avisi-cloud) é a
      # ferramenta mantida para gerar HTML estático a partir de workspace.dsl.
      # Monta apenas a pasta structurizr/ para excluir o symlink docs/ e manter
      # a exportação C4 separada da documentação MkDocs.
      - name: Exportar C4 — HTML estático (Structurizr)
        run: |
          mkdir -p /tmp/c4-model site/c4
          cp structurizr/workspace.dsl  /tmp/c4-model/
          cp structurizr/overview.md    /tmp/c4-model/
          cp structurizr/workspace.json /tmp/c4-model/ 2>/dev/null || true
          cp structurizr/theme.json     /tmp/c4-model/ 2>/dev/null || true
          docker run --rm \
            --user "$(id -u):$(id -g)" \
            -v "/tmp/c4-model:/var/model" \
            ghcr.io/avisi-cloud/structurizr-site-generatr:latest \
            generate-site --workspace-file workspace.dsl
          cp -r /tmp/c4-model/build/site/* site/c4/

      # ── Relatórios de testes (último CI verde em main) ──────────────────────
      - name: Baixar relatório — lancamentos
        uses: dawidd6/action-download-artifact@v9
        with:
          workflow: ci.yml
          branch: main
          name: test-results-lancamentos
          path: site/tests/lancamentos
          if_no_artifact_found: warn

      - name: Baixar relatório — consolidado
        uses: dawidd6/action-download-artifact@v9
        with:
          workflow: ci.yml
          branch: main
          name: test-results-consolidado
          path: site/tests/consolidado
          if_no_artifact_found: warn

      # ── Cobertura JaCoCo (último CI verde em main) ──────────────────────────
      - name: Baixar cobertura — lancamentos
        uses: dawidd6/action-download-artifact@v9
        with:
          workflow: ci.yml
          branch: main
          name: coverage-lancamentos
          path: site/coverage/lancamentos
          if_no_artifact_found: warn

      - name: Baixar cobertura — consolidado
        uses: dawidd6/action-download-artifact@v9
        with:
          workflow: ci.yml
          branch: main
          name: coverage-consolidado
          path: site/coverage/consolidado
          if_no_artifact_found: warn

      # ── Infracost (último CI verde em main) ─────────────────────────────────
      - name: Baixar relatório Infracost
        uses: dawidd6/action-download-artifact@v9
        with:
          workflow: ci.yml
          branch: main
          name: infracost-report
          path: site/infracost
          if_no_artifact_found: warn

      - name: Fallback — páginas indisponíveis
        run: |
          if [ ! -f site/infracost/index.html ]; then
            mkdir -p site/infracost
            echo '<meta charset="utf-8"><p>Relatório não disponível. Configure o secret <code>INFRACOST_API_KEY</code> no repositório.</p>' > site/infracost/index.html
          fi
          if [ ! -f site/tests/lancamentos/index.html ]; then
            mkdir -p site/tests/lancamentos
            echo '<meta charset="utf-8"><p>Relatório não disponível. Execute o CI em main para gerar os artefatos de teste.</p>' > site/tests/lancamentos/index.html
          fi
          if [ ! -f site/tests/consolidado/index.html ]; then
            mkdir -p site/tests/consolidado
            echo '<meta charset="utf-8"><p>Relatório não disponível. Execute o CI em main para gerar os artefatos de teste.</p>' > site/tests/consolidado/index.html
          fi
          if [ ! -f site/coverage/lancamentos/index.html ]; then
            mkdir -p site/coverage/lancamentos
            echo '<meta charset="utf-8"><p>Cobertura não disponível. Execute o CI em main para gerar os artefatos.</p>' > site/coverage/lancamentos/index.html
          fi
          if [ ! -f site/coverage/consolidado/index.html ]; then
            mkdir -p site/coverage/consolidado
            echo '<meta charset="utf-8"><p>Cobertura não disponível. Execute o CI em main para gerar os artefatos.</p>' > site/coverage/consolidado/index.html
          fi

      - name: Upload artefato para Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: site/

  deploy:
    name: Publicar no GitHub Pages
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy para GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
